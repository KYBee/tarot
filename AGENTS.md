# AGENTS.md

이 문서는 `/Users/kybee/dev/projects/tarot` 아래 전체에 적용된다.

## 프로젝트 정체성

- 서비스명: `너의 타로는?`
- 성격: 모바일 우선 타로 프로필 웹 서비스
- 목표: "계산기"처럼 보이지 않고, 생년월일 기반의 타로 프로필을 읽는 경험을 준다.
- 톤앤매너: 신비롭고 우주적인 무드, 딥 퍼플 배경, 골드 포인트, 세련된 감성. 점집 느낌은 피한다.

## 가장 먼저 볼 문서

작업 전에 아래 순서로 읽는다.

1. `docs/README.md`
2. `docs/product-overview.md`
3. `docs/requirements.md`
4. `docs/calculation-rules.md`
5. `docs/content-model.md`
6. `docs/content-writing-guide.md`
7. `docs/ui-ux-spec.md`
8. `docs/technical-plan.md`
9. `docs/kakao-share-integration.md`
10. `docs/content-cards.md`
11. `docs/implementation-checklist.md`

문서와 코드가 충돌하면:

- 작은 불일치는 문서 기준으로 고친다.
- 문서와 구현 사이에 애매함이 있으면 임의 해석으로 밀어붙이지 말고, 코드에 짧은 `TODO` 또는 주석으로 남기고 사용자에게 알린다.

## 현재 구현 상태

현재 MVP는 이미 구현되어 있다.

- 입력 화면, 로딩 화면, 결과 화면이 동작한다.
- 계산 로직은 문서 기준으로 구현되어 있다.
- 탄생카드 / 페르소나 카드 / 작년·올해·내년 연도 카드가 렌더링된다.
- 별도 페르소나 카드가 나오지 않을 때는 탄생카드와 같은 카드를 쓰는 `탄생·페르소나 통합형` 상태를 보여준다.
- 카드 이미지 세트는 `img/` 디렉터리에 연결되어 있다.
- 카카오 공유는 아직 실제 SDK 미연동이며 fallback만 구현되어 있다.

즉, 다음 작업은 "처음부터 만들기"가 아니라 "문서 기준 유지 + 기존 MVP 점진 개선"이어야 한다.

## 반드시 지킬 구현 원칙

- 현재 구조 `index.html / script.js / style.css`는 유지한다.
- 프레임워크로 갈아엎지 않는다.
- 함수 책임은 나누되, 과한 추상화로 구조를 무겁게 만들지 않는다.
- 기능 완성 > 구조 정리 > 세부 polish 순서로 우선순위를 둔다.
- mock 분위기와 현재 결과 화면 흐름은 최대한 유지한다.
- 시각적 수정이 있어도 모바일 우선 구조를 깨지 않는다.
- 디자인 방향을 함부로 바꾸지 않는다. 사용자가 명시적으로 요청하지 않으면 현재 우주적/카드형 디렉션을 유지한다.
- 필요한 부분만 고친다. 작업 범위를 벗어나는 정리나 리디자인은 하지 않는다.
- 문서를 과장되게 쓰지 않는다. 과한 마케팅 문구, 과장된 성과 표현, 실제보다 크게 말하는 표현을 피한다.
- 커밋은 가능한 한 작은 단위로 나눈다. 서로 다른 성격의 변경을 한 커밋에 섞지 않는다.
- 이 저장소는 현재 초기 메인 반영 단계다. 지금 이후의 후속 작업은 기본적으로 브랜치 + PR 방식으로 진행한다.

## 파일별 역할

### `index.html`

- 화면 구조와 DOM anchor를 가진다.
- 랜딩 / 로딩 / 결과 3개 스크린 구조를 유지한다.
- 결과 화면은 스와이프 카드 2장 + 연도 흐름 + 공유/재시작 액션 구조를 유지한다.

### `script.js`

- 계산 로직, 렌더링 로직, 이벤트 바인딩, share fallback을 가진다.
- 테스트 가능한 순수 함수가 이미 일부 분리되어 있다.
- 브라우저 환경과 Node 테스트 환경에서 둘 다 로드될 수 있게 작성되어 있다.

### `style.css`

- 현재 서비스의 실제 look & feel을 결정한다.
- 딥 퍼플 / 골드 / 글래스 카드 / 우주적 배경 방향을 유지한다.
- 텍스트가 길어도 읽히는 line-height, spacing, overlay 처리를 우선한다.

### `data/card-content.js`

- 카드 콘텐츠와 공통 문구의 single source of truth다.
- 카드 텍스트를 수정할 때는 이 파일과 `docs/content-cards.md`를 함께 맞춘다.

### `img/`

- 메이저 아르카나 0~21 카드 이미지 세트가 들어 있다.
- 현재 `script.js`의 `CARD_IMAGE_FILES` 매핑이 이 디렉터리를 기준으로 동작한다.
- 새 이미지를 교체해도 파일명 규칙이 바뀌면 매핑도 함께 수정해야 한다.

### `tests/script.test.js`

- 핵심 계산 로직과 이미지 매핑의 최소 회귀 테스트가 있다.
- 계산 규칙을 바꾸면 테스트도 반드시 같이 수정한다.

## 계산 규칙

반드시 `docs/calculation-rules.md`를 기준으로 한다.

- 탄생카드:
  - `YYYY + MM + DD`
  - 자리수 합을 반복
  - 최종 한 자리(`1~9`)가 탄생카드
- 페르소나 카드:
  - 축약 과정에서 처음 나온 `10~22`
  - 없으면 `null`
  - `null`이면 UI에서는 탄생카드와 같은 카드로 이어지는 `탄생·페르소나 통합형`으로 표시
- 연도 카드:
  - `MM + DD + 대상 연도`
  - `22` 이하가 될 때까지 자리수 축약
  - `22`는 canonical `0`

표시 정책:

- 내부 canonical 번호는 `0~21`
- UI에서는 바보 카드를 `0/22`로 표기할 수 있다.

이 규칙은 임의 변경 금지다.

## 콘텐츠 규칙

- 카드 설명은 다음 구조를 따른다.
  - 카드 타입 설명
  - 기본 의미
  - 그림 속 상징
  - 왜 이렇게 읽을까요?
  - 역할별 프로필 설명
  - 연도 흐름 해석
- 탄생/페르소나 카드의 설명과 연도 카드의 설명은 반드시 분리한다.
- 문체는 `docs/content-writing-guide.md`를 따른다.
- 너무 예언문처럼 쓰지 않는다.
- 차갑고 번역투 같은 문장을 피하고, 한국어 서비스 톤으로 다듬는다.

## UI / UX 규칙

- 모바일 우선이다.
- 결과 화면의 카드형 흐름은 유지한다.
- 스와이프 카드 2장 구조를 함부로 제거하지 않는다.
- 긴 텍스트가 들어와도 카드가 무너지지 않도록 spacing과 overlay를 조정한다.
- 카드 이미지를 쓰더라도 텍스트 가독성을 희생하지 않는다.
- `cards/21.png`는 초기 참고 자산이고, 실제 세트는 `img/`가 우선이다.

## 카카오 공유 규칙

- 현재는 fallback만 구현돼 있다.
- 실제 Kakao SDK 연동은 아직 TODO다.
- 연동 전에는 기존 fallback 구조를 망가뜨리지 않는다.
- 실제 키/도메인 연동 시 `docs/kakao-share-integration.md`를 먼저 읽는다.

## 작업 시 주의사항

- 문서 없이 코드부터 뜯지 않는다.
- 대규모 리팩터링 금지.
- 기존 DOM id를 함부로 바꾸지 않는다. `script.js`가 직접 참조한다.
- 이미지 파일명 규칙을 바꾸면 `CARD_IMAGE_FILES`도 함께 수정해야 한다.
- `bg.png`는 루트에 있다. 배경 경로를 바꿀 때 경로 기준을 확인한다.
- 브라우저에서 스타일이 안 먹는 것처럼 보이면 먼저 실제 서버/포트와 정적 파일 경로가 맞는지 확인한다.
- 특별한 이유가 없으면 main에 직접 푸시하지 않는다. 초기 반영 이후 작업은 PR을 기본으로 한다.

## 검증 명령

작업 후 최소한 아래는 다시 돌린다.

```bash
node --test tests/script.test.js
node --check script.js
node --check data/card-content.js
```

가능하면 추가로 확인한다.

- `index.html`, `style.css`, `script.js`, `data/card-content.js`의 진단 에러 여부
- 실제 브라우저에서 입력 -> 결과 -> 스와이프 -> 다시 하기 -> 공유 fallback 흐름

## 현재 남아 있는 TODO

- 실제 Kakao JavaScript SDK 연동
- 공유 payload에 고정 결과 URL을 넣는 구조
- 추가 회귀 테스트 보강:
  - `22 -> 0/22` 정규화 케이스
  - 탄생·페르소나 통합형 렌더링 케이스
- 시각 polish와 모바일 QA

## 다음 에이전트에게

- 이 저장소는 이미 "기획 문서화 -> MVP 구현"이 한 번 끝난 상태다.
- 다음 작업은 이 기반 위에서 이어가는 보수/개선 작업이어야 한다.
- 사용자가 별도 디렉션을 주면 그 내용을 우선하되, 문서와 현재 구현의 축을 동시에 맞춰라.
